# UE build artifacts
Binaries/
Intermediate/
DerivedDataCache/
Saved/

# OS / editor noise
.vs/
.vscode/
*.pdb
*.suo
*.user
Thumbs.db
.DS_Store

# Never commit game-derived data (see AGENTS.md hard walls)
*.ydr*
*.ybn*
*.ytd*
*.ytyp*
*.ymap*
*.ydd*
*.yft*
*.ymt*
*.dds
*.rpf

# Generated on the user's machine from their own game files - NEVER committed.
# RUDE's front page promises this repository carries no game data (README, the limits table),
# and a district catalog is derived from the game's own ymap families and extents.
# BuildAreaCatalog writes it; ImportArea reads it; git must never see it.
Catalogs/
